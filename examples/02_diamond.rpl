// 示例 02：菱形依赖
// 功能：演示无故障传播（Glitch-Free）
// 输入：A=1 -> A=5
// 输出：A=1 时 D=4，A=5 时 D=16（一次计算，无中间状态）

source A : int := 1;

stream B <- A * 2;      // B 依赖 A
stream C <- A + 1;      // C 依赖 A
stream D <- B + C;      // D 依赖 B 和 C（菱形结构）

sink output <- D;

// 测试：
// 初始 A=1: B=2, C=2, D=4
// 推送 A=5: B=10, C=6, D=16
// 保证：D 只计算一次，不会看到 B=10, C=2 的中间状态
