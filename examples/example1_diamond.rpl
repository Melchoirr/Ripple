// Example 1: Diamond Dependency (Glitch-Free Propagation)
// Demonstrates how Ripple handles diamond dependencies without glitches

source A : int := 1;

stream B <- A * 2;      // B depends on A
stream C <- A + 1;      // C depends on A
stream D <- B + C;      // D depends on both B and C (diamond)

sink output <- D;

// When A=1: B=2, C=2, D=4
// When A=5: B=10, C=6, D=16 (computed once, no intermediate state)
